#include "main.h"
#include <stdio.h>

/**
 * int mul - multiplies 2 integers
 * @a & @b: integers to be multiplies
 * Return: returns multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}
